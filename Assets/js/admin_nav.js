// document.addEventListener("DOMContentLoaded", function() {
//     const element = document.getElementById("title_bar");
//     const fullText = element.innerText;
//     const maxLength = 40; // Độ dài tối đa cần giữ nguyên đầu và cuối

//     if (fullText.length > maxLength) {
//         const startText = fullText.slice(0, 30); // Phần đầu
//         const endText = fullText.slice(-20); // Phần cuối
//         element.innerText = `${startText}...${endText}`;
//     }
// });
